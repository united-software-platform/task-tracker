{
    "name": "req-control/app",
    "description": "Requirements Control Platform",
    "version": "1.0.22",
    "type": "project",
    "require": {
        "php": "^8.4",
        "mcp/sdk": "^0.4.0",
        "ext-pdo": "*"
    },
    "require-dev": {
        "deptrac/deptrac": "^2.0",
        "friendsofphp/php-cs-fixer": "^3.70",
        "phpstan/phpstan": "^2.1",
        "phpunit/phpunit": "^12.1"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "lint": "php-cs-fixer fix --dry-run --diff",
        "lint:fix": "php-cs-fixer fix",
        "analyse": "phpstan analyse",
        "arch": "deptrac analyse --config-file=deptrac.php",
        "test": "phpunit",
        "test:unit": "phpunit --testsuite Unit",
        "hooks:install": "git config core.hooksPath .githooks && chmod +x .githooks/pre-commit"
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "php-http/discovery": true,
            "phpstan/extension-installer": true
        }
    }
}
